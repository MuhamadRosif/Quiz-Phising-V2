# Quiz App (Modular) — Netlify + Supabase ready

Instruksi:
1. Buat project folder.
2. Copy semua file sesuai struktur.
3. `npm install`
4. Buat project Supabase, buat tabel sesuai `supabase-schema.sql`.
5. Tambahkan env vars di `.env` atau Netlify:
   - VITE_SUPABASE_URL
   - VITE_SUPABASE_ANON_KEY
   - VITE_ADMIN_PASSWORD
6. `npm run dev`

Deployment: push ke GitHub -> connect ke Netlify -> set env vars -> build.
